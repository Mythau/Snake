#include <chrono>
#include <iostream>
#include "Header.h"



int main()
{

	GameMap MapMeDaddy;
	MapMeDaddy.PrintMap();





/*	while (GameOver != true)
	{
		run gamestate
			std::this_thread::sleep_for(std::chrono::milliseconds(WaitPeriod));

	}
*/
}
